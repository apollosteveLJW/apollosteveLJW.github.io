auto a = 10;
*/which is used in lambda function mostly*/
auto f = [&](int x, int y) {
  std::cout << x + y << std::endl;
};
//or something you can not judge the type
auto std::async(std::launch::async, f, 40, 41);
推到发生在编译阶段
